[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    e2e: Playwright end-to-end tests (require --headed or --browser)
    unit: Fast unit/integration tests
